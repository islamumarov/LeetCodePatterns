namespace DailyLeetCode;

public static class MinimumChangesToMakeAlternatingBinaryString
{
    public static int MinOperations(string s)
    {
        int operations = 0;
        var current = s[0];
        for (int i = 1; i < s.Length - 1; i++)
        {
            
        }

        return operations;
    }
}